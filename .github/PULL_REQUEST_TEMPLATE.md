## What this changes

<!-- A sentence or two. Link the issue if there is one. -->

## How it was verified

<!--
What you actually ran. For visual or animation changes, say which model and
which clips you exercised, and attach a screenshot or capture if you can.
-->

## Checklist

- [ ] `npm test`, `npm run typecheck`, and `npm run lint` pass
- [ ] New public exports are re-exported from `src/index.ts`
- [ ] Relative imports inside `src/` use explicit `.js` extensions
- [ ] The `procAnim` snapshot is unchanged — or the change is intentional and
      explained above
